# 三角色能力图标全量重绘发布

用户于 2026-09-11 批准覆盖正式资源并提交 Git。

- 114 张图标：守护者 53、铁之眼 33、复仇者 28；不含女爵及隐藏召唤控制器。
- 主体均由内置 imagegen 生成；本地处理仅用于背景透明化、留白和等比缩放。正式图标全部为 256×256 RGBA PNG。
- 守护者以 `guardian_assets/guardian_power_atlas.png` 为唯一源，53 个 256px 图集区域带隔离间距；同步到 `images/powers` 与 `powers`。
- 复仇者同步 `revenant_assets/powers`、`images/powers` 与 `powers`；铁之眼独立 PNG 同步两处大图路径。
- 保留所有同名 AtlasTexture 映射。此次不改动卡牌机制、本地化、头像、音效或女爵资源。
- 同步脚本现在先隔离单元格再采样，256px 单元格直接复制像素，避免相邻图标串色及透明边缘变化。

对应 JSON 记录批准图标的名称、ID、源文件、图集区域和像素哈希。后续批准替换图标时应同步更新对应记录。

## 导出验证

先运行正常导出 `tools/export_guardian_mod.ps1 -SkipInstall`，再在不含游戏源资源的验证项目中挂载 PCK：

```powershell
& 'D:/Godot_v4.5.1-stable_mono_win64/Godot_v4.5.1-stable_mono_win64.exe' --headless --path tools/power_icon_validation --log-file '<绝对路径>/build/power_icon_validation.log' --script res://validate.gd -- '<绝对路径>/build/NightMustStay.pck' '<绝对路径>/design/能力图标全量重绘发布_20260911.json'
```

成功标志为 `POWER_PACK_VALIDATION resources=342 powers=114 failed=0`。该检查覆盖每个图标的紧凑图集、解释框和触发大图，并核对批准源的 Alpha/主体像素和三个消费者的一致性。

替换前的本地文件备份位于 `.tmp/power_icons_publish_20260911/before`，不随 Git 发布。

本次已完成正式 PCK 导出、342 个图标资源隔离挂载验证及 Release 构建（0 警告、0 错误）。验证未安装到本机游戏 mods 目录。沙箱内构建最初无法访问 Godot/NuGet 缓存，使用本机已有 `Godot.NET.Sdk/4.5.1` 缓存后通过，无需更改项目依赖。
